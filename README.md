# Nemu - تطبيق تجارة إلكترونية

تطبيق تجارة إلكترونية مشابه لـ Temu مبني باستخدام Expo React Native و Supabase و TypeScript.

## المميزات

- 📱 تطبيق متعدد المنصات (iOS, Android, Web)
- 🛒 نظام سلة تسوق كامل
- 📦 إدارة الطلبات والشحنات
- 👤 نظام مستخدمين مع أدوار مختلفة (عميل، موظف، مدير، أدمن)
- 🔔 نظام إشعارات
- 📍 تحديد الموقع التلقائي
- 🖼️ رفع الصور على ImgBB
- 🎨 تصميم مشابه لـ Temu بألوانه

## التقنيات المستخدمة

- **Expo** - إطار عمل React Native
- **React Native** - بناء تطبيقات الموبايل
- **TypeScript** - لغة البرمجة
- **Supabase** - قاعدة البيانات والـ Backend
- **Expo Router** - التنقل بين الصفحات
- **ImgBB API** - رفع الصور

## الإعداد

### 1. تثبيت المتطلبات

```bash
npm install
```

### 2. إعداد Supabase

1. أنشئ مشروع جديد على [Supabase](https://supabase.com)
2. انسخ ملف `supabase/schema.sql` وقم بتنفيذه في SQL Editor
3. أنشئ Edge Functions من مجلد `supabase/functions`

### 3. إعداد المتغيرات البيئية

أنشئ ملف `.env` في المجلد الرئيسي:

```env
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
EXPO_PUBLIC_IMGBB_API_KEY=your_imgbb_api_key
```

### 4. تشغيل التطبيق

```bash
# تشغيل على iOS
npm run ios

# تشغيل على Android
npm run android

# تشغيل على Web
npm run web
```

## هيكل المشروع

```
nemu/
├── app/                    # صفحات التطبيق (Expo Router)
│   ├── (tabs)/            # صفحات التبويبات
│   │   ├── index.tsx      # الرئيسية
│   │   ├── cart.tsx       # السلة
│   │   ├── profile.tsx    # الملف الشخصي
│   │   └── admin.tsx      # الإدارة
│   ├── auth.tsx           # المصادقة
│   └── product/[id].tsx    # تفاصيل المنتج
├── lib/                    # المكتبات المساعدة
│   ├── supabase.ts        # إعداد Supabase
│   └── imgbb.ts           # رفع الصور
├── types/                  # تعريفات TypeScript
│   └── index.ts
├── supabase/               # ملفات Supabase
│   ├── schema.sql         # مخطط قاعدة البيانات
│   └── functions/         # Edge Functions
└── package.json
```

## الأدوار والصلاحيات

- **customer**: العميل - يمكنه التصفح والشراء
- **employee**: الموظف - يمكنه عرض الطلبات والشحنات
- **manager**: المدير - يمكنه إدارة الشحنات والمخزون
- **admin**: الأدمن - صلاحيات كاملة

## تدفق العمل

1. العميل يتصفح المنتجات ويضيف للسلة
2. يؤكد الطلب (يتم سحب الموقع تلقائياً أو يدوياً)
3. المدير يستلم إشعار بطلب جديد
4. المدير يطلب من الصين ويُنشئ شحنة
5. الشحنة تذهب للإمارات
6. الموظف في الإمارات يستلم الشحنة
7. الشحنة تُشحن من الإمارات لمصر
8. الشحنة تصل مصر وتدخل المخزن
9. بعد الفرز، يتم إشعار العملاء بموعد الوصول

## Edge Functions

التطبيق يستخدم Supabase Edge Functions للعمليات المعقدة:

- `create-order`: إنشاء طلب جديد
- `confirm-order`: تأكيد الطلب
- `update-shipment-status`: تحديث حالة الشحنة

## رفع الصور

الصور تُرفع على ImgBB API ويتم حفظ الرابط في قاعدة البيانات لتوفير المساحة.

## الترخيص

MIT

