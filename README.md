# Nemu - E-commerce Platform

منصة تجارة إلكترونية متكاملة مع إدارة المنتجات، الطلبات، الفئات، والمستخدمين.

## المميزات

- 🛍️ **إدارة المنتجات**: إضافة، تعديل، وحذف المنتجات مع صور متعددة
- 📦 **إدارة الفئات**: نظام فئات متكامل مع أيقونات وترتيب مخصص
- 🛒 **سلة التسوق**: إضافة المنتجات للسلة وإنشاء الطلبات
- 👥 **إدارة المستخدمين**: نظام أدوار (مستخدم، موظف، مدير، أدمن)
- 💰 **نظام الخصومات**: إدارة الأسعار الأصلية ونسب الخصومات
- 📱 **Responsive Design**: يعمل على الويب والموبايل
- 🔐 **Authentication**: تسجيل دخول آمن مع Supabase

## التقنيات المستخدمة

- **Frontend**: React Native (Expo) + TypeScript
- **Backend**: Supabase (PostgreSQL + Auth + Storage)
- **Deployment**: Vercel / Netlify (للويب)

## الإعداد الأولي

### 1. تثبيت المتطلبات

```bash
npm install
```

### 2. إعداد متغيرات البيئة

أنشئ ملف `.env` في المجلد الرئيسي:

```env
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
EXPO_PUBLIC_IMGBB_API_KEY=your_imgbb_api_key
EXPO_SUPABASE_TOKEN=your_supabase_access_token
```

### 3. تشغيل المشروع

```bash
# للويب
npm run web

# للموبايل
npm start
```

## النشر

### نشر على Vercel (موصى به)

1. سجّل حساب على [Vercel](https://vercel.com)
2. ثبت Vercel CLI:
   ```bash
   npm i -g vercel
   ```
3. سجّل الدخول:
   ```bash
   vercel login
   ```
4. انشر المشروع:
   ```bash
   vercel
   ```
5. أضف متغيرات البيئة في Vercel Dashboard

### نشر على Netlify

1. سجّل حساب على [Netlify](https://netlify.com)
2. ثبت Netlify CLI:
   ```bash
   npm i -g netlify-cli
   ```
3. انشر المشروع:
   ```bash
   netlify deploy --prod
   ```

### نشر على GitHub Pages

1. ارفع المشروع على GitHub
2. في إعدادات Repository → Pages
3. اختر Branch: `main` و Folder: `/web-build`

## هيكل المشروع

```
nemu/
├── app/                    # صفحات التطبيق
│   ├── (tabs)/            # التبويبات الرئيسية
│   │   ├── index.tsx      # الصفحة الرئيسية
│   │   ├── cart.tsx       # سلة التسوق
│   │   ├── profile.tsx   # الملف الشخصي
│   │   └── admin.tsx     # لوحة الإدارة
│   ├── auth.tsx           # تسجيل الدخول
│   └── product/[id].tsx   # صفحة المنتج
├── contexts/              # React Contexts
├── lib/                   # مكتبات مساعدة
├── types/                 # TypeScript Types
├── supabase/              # SQL Migrations
└── scripts/               # Scripts مساعدة
```

## قاعدة البيانات

المشروع يستخدم Supabase PostgreSQL مع الجداول التالية:

- `users` - المستخدمون
- `products` - المنتجات
- `categories` - الفئات
- `product_images` - صور المنتجات
- `orders` - الطلبات
- `order_items` - عناصر الطلبات
- `shipments` - الشحنات

## التطوير المستقبلي

- [ ] تطبيق Android (Google Play)
- [ ] تطبيق iOS (App Store)
- [ ] نظام الإشعارات
- [ ] نظام التقييمات
- [ ] نظام الدفع الإلكتروني

## الترخيص

هذا المشروع خاص.

## الدعم

للأسئلة والدعم، يرجى فتح Issue على GitHub.
