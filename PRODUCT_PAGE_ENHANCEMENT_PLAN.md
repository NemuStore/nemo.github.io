# خطة تحسين صفحة المنتج (مستوحاة من Temu)

## ⚠️ متطلبات إضافية مهمة

### 1. التمييز بين المنتجات الداخلية والخارجية
- **منتجات المخزن الداخلي** (`source_type = 'warehouse'`): لها مخزون محدد
- **منتجات مطلوبة من الخارج** (`source_type = 'external'`): لا تحتاج مخزون

### 2. فصل الطلبات
- عند طلب منتجات من **الداخل والخارج** معاً، يتم فصلها إلى:
  - **طلب منفصل للمنتجات الداخلية**
  - **طلب منفصل للمنتجات الخارجية**

### 3. كود المنتج (SKU)
- **SKU فريد** لكل منتج (UNIQUE constraint)
- **لا يظهر للعملاء** - للإدارة فقط

## 📊 1. ما سيضاف في قاعدة البيانات

### أ. جدول `product_specifications` (مواصفات المنتج)
```sql
- id (UUID)
- product_id (UUID) → FK to products
- spec_type (TEXT) → 'color', 'size', 'material', 'dimensions', etc.
- spec_key (TEXT) → 'اللون', 'المقاس', 'الخامة', etc.
- spec_value (TEXT) → القيمة الفعلية
- display_order (INTEGER)
- created_at, updated_at
```

**الاستخدام:**
- الألوان المتاحة (أحمر، أزرق، أسود)
- المقاسات (S, M, L, XL)
- الخامة (قطن، بوليستر)
- الأبعاد (طول × عرض × ارتفاع)

---

### ب. جدول `product_variants` (متغيرات المنتج - اختياري)
```sql
- id (UUID)
- product_id (UUID) → FK to products
- variant_name (TEXT) → 'أحمر - مقاس L'
- color (TEXT) → NULL
- size (TEXT) → NULL
- price (DECIMAL) → سعر مختلف للمتغير (اختياري)
- stock_quantity (INTEGER) → مخزون خاص بهذا المتغير
- sku (TEXT) → كود المنتج الفريد
- image_url (TEXT) → صورة خاصة بهذا المتغير
- is_active (BOOLEAN)
- created_at, updated_at
```

**الاستخدام:**
- إذا كان المنتج له ألوان ومقاسات مختلفة
- كل متغير له سعر ومخزون منفصل

---

### ج. جدول `product_reviews` (تقييمات ومراجعات)
```sql
- id (UUID)
- product_id (UUID) → FK to products
- user_id (UUID) → FK to users
- rating (INTEGER) → 1-5
- title (TEXT) → عنوان المراجعة
- comment (TEXT) → نص المراجعة
- images (TEXT[]) → صور من العميل (اختياري)
- is_verified_purchase (BOOLEAN) → شراء مؤكد
- helpful_count (INTEGER) → عدد من وجدوها مفيدة
- created_at, updated_at
```

---

### د. جدول `product_shipping_info` (معلومات الشحن)
```sql
- id (UUID)
- product_id (UUID) → FK to products
- shipping_cost (DECIMAL) → تكلفة الشحن (NULL = مجاني)
- estimated_delivery_days (INTEGER) → أيام التوصيل المتوقعة
- free_shipping_threshold (DECIMAL) → الحد الأدنى للشحن المجاني
- return_policy_days (INTEGER) → أيام الإرجاع المسموحة
- warranty_period (TEXT) → فترة الضمان
- created_at, updated_at
```

**ملاحظة:** يمكن إضافة هذه الحقول مباشرة في جدول `products` بدلاً من جدول منفصل (أبسط)

---

### هـ. تحديث جدول `products` (إضافة حقول جديدة)
```sql
-- إضافة حقول جديدة لجدول products الموجود
ALTER TABLE products ADD COLUMN IF NOT EXISTS:
- shipping_cost DECIMAL(10, 2) NULL
- estimated_delivery_days INTEGER NULL
- free_shipping_threshold DECIMAL(10, 2) NULL
- return_policy_days INTEGER NULL
- warranty_period TEXT NULL
- weight_kg DECIMAL(5, 2) NULL → الوزن بالكيلو
- dimensions TEXT NULL → '30x20x10 سم'
- brand TEXT NULL → العلامة التجارية
- sku TEXT NULL → كود المنتج
- is_featured BOOLEAN DEFAULT false → منتج مميز
- is_new BOOLEAN DEFAULT false → منتج جديد
- tags TEXT[] → ['عروض', 'جديد', 'مميز']
```

---

## 📝 2. ما سيضاف في نموذج إضافة/تعديل المنتج

### أ. قسم المعلومات الأساسية (موجود + جديد)
- ✅ اسم المنتج
- ✅ الوصف
- ✅ السعر
- ✅ السعر الأصلي (للخصم)
- ✅ نسبة الخصم
- 🆕 **مصدر المنتج** (مخزن داخلي / طلب من الخارج) - **مطلوب**
- 🆕 **كود المنتج (SKU)** - **مطلوب وفريد**
- ✅ الكمية في المخزن (يظهر فقط للمنتجات الداخلية)
- ✅ الفئة
- ✅ الصور

### ب. قسم جديد: مواصفات المنتج
```
┌─────────────────────────────────────┐
│ مواصفات المنتج                      │
├─────────────────────────────────────┤
│ [إضافة مواصفة جديدة]                │
│                                      │
│ نوع المواصفة: [قائمة منسدلة]       │
│   - اللون                            │
│   - المقاس                           │
│   - الخامة                           │
│   - الأبعاد                          │
│   - أخرى (مخصص)                      │
│                                      │
│ القيمة: [حقل نص]                    │
│                                      │
│ [حفظ] [إلغاء]                       │
└─────────────────────────────────────┘

المواصفات المضافة:
- اللون: أحمر، أزرق، أسود
- المقاس: S, M, L, XL
- الخامة: قطن 100%
```

### ج. قسم جديد: معلومات الشحن والتوصيل
```
┌─────────────────────────────────────┐
│ معلومات الشحن والتوصيل              │
├─────────────────────────────────────┤
│ تكلفة الشحن: [حقل رقم] ج.م         │
│   (اتركه فارغاً للشحن المجاني)      │
│                                      │
│ أيام التوصيل المتوقعة: [حقل رقم]   │
│                                      │
│ الحد الأدنى للشحن المجاني: [رقم]   │
│   (إذا كان الطلب أكبر من هذا المبلغ)│
│                                      │
│ أيام الإرجاع المسموحة: [حقل رقم]   │
│                                      │
│ فترة الضمان: [حقل نص]              │
│   مثال: "سنة واحدة"                 │
└─────────────────────────────────────┘
```

### د. قسم جديد: معلومات إضافية
```
┌─────────────────────────────────────┐
│ معلومات إضافية                     │
├─────────────────────────────────────┤
│ الوزن (كجم): [حقل رقم]             │
│                                      │
│ الأبعاد: [حقل نص]                   │
│   مثال: "30x20x10 سم"               │
│                                      │
│ العلامة التجارية: [حقل نص]         │
│                                      │
│ كود المنتج (SKU): [حقل نص]         │
│                                      │
│ [☐] منتج مميز                       │
│ [☐] منتج جديد                       │
│                                      │
│ العلامات (Tags):                    │
│   [أضف علامة]                       │
│   [عروض] [جديد] [مميز] [×]          │
└─────────────────────────────────────┘
```

---

## 🏷️ 3. ما سيضاف في الأقسام والفئات

### أ. في الأقسام (Sections)
**لا حاجة لإضافات كبيرة** - الأقسام بسيطة:
- ✅ الاسم
- ✅ الوصف
- ✅ الأيقونة
- ✅ الترتيب
- ✅ الحالة (نشط/غير نشط)

**اقتراح إضافي (اختياري):**
- `banner_image` → صورة بانر للقسم
- `display_style` → 'grid', 'list', 'carousel'

---

### ب. في الفئات (Categories)
**إضافات مقترحة:**
```
┌─────────────────────────────────────┐
│ معلومات الفئة                       │
├─────────────────────────────────────┤
│ ✅ الاسم                             │
│ ✅ الوصف                             │
│ ✅ الأيقونة                          │
│ ✅ القسم (الأب)                      │
│ ✅ الترتيب                           │
│ ✅ الحالة                            │
│                                      │
│ 🆕 صورة البانر: [رفع صورة]          │
│ 🆕 لون الفئة: [color picker]         │
│ 🆕 فلاتر افتراضية:                   │
│   [☐] حسب السعر                      │
│   [☐] حسب المقاس                    │
│   [☐] حسب اللون                      │
└─────────────────────────────────────┘
```

**في قاعدة البيانات:**
```sql
ALTER TABLE categories ADD COLUMN IF NOT EXISTS:
- banner_image TEXT NULL
- color_hex TEXT NULL → '#FF5733'
- default_filters JSONB NULL → {'price': true, 'size': true}
```

---

## 🎯 4. أولويات التنفيذ

### المرحلة 1: الأساسيات (أولوية عالية)
1. ✅ إضافة حقول الشحن في جدول `products`
2. ✅ إضافة حقول المواصفات الأساسية (الوزن، الأبعاد، SKU)
3. ✅ تحديث نموذج إضافة المنتج
4. ✅ تحديث صفحة عرض المنتج

### المرحلة 2: المواصفات (أولوية متوسطة)
5. ✅ إنشاء جدول `product_specifications`
6. ✅ إضافة واجهة إدارة المواصفات في نموذج المنتج
7. ✅ عرض المواصفات في صفحة المنتج

### المرحلة 3: التقييمات (أولوية متوسطة)
8. ✅ إنشاء جدول `product_reviews`
9. ✅ واجهة إضافة تقييم
10. ✅ عرض التقييمات في صفحة المنتج

### المرحلة 4: المتغيرات (أولوية منخفضة - اختياري)
11. ⏸️ إنشاء جدول `product_variants` (إذا لزم الأمر)
12. ⏸️ واجهة إدارة المتغيرات

---

## 📋 5. ملخص الجداول الجديدة

### جداول جديدة:
1. `product_specifications` → مواصفات المنتج
2. `product_reviews` → تقييمات العملاء
3. `product_variants` → متغيرات المنتج (اختياري)

### تحديثات على جداول موجودة:
1. `products` → إضافة حقول الشحن والمعلومات الإضافية
2. `categories` → إضافة حقول البانر والألوان (اختياري)

---

## 🚀 الخطوات التالية

1. **إنشاء ملف SQL للتحديثات**
2. **تحديث TypeScript types**
3. **تحديث نموذج إضافة المنتج**
4. **تحديث صفحة عرض المنتج**
5. **إضافة عناصر تعزيز الثقة (شحن مجاني، إرجاع سهل)**

---

## 💡 ملاحظات مهمة

- **البداية البسيطة:** ابدأ بإضافة الحقول مباشرة في جدول `products` قبل إنشاء جداول منفصلة
- **التدرج:** لا تحاول إضافة كل شيء مرة واحدة
- **الاختبار:** اختبر كل مرحلة قبل الانتقال للتالية
- **المرونة:** بعض الحقول يمكن أن تكون NULL (اختيارية)

