# 🚀 كيفية تشغيل التطبيق

## الطريقة 1: من Terminal (الأسهل)

التطبيق يعمل بالفعل! افتح Terminal جديد واكتب:

```bash
cd /home/zero/Desktop/nemu
npm start
```

ستظهر لك قائمة، اضغط:
- **`w`** - لفتح في المتصفح (Web)
- **`a`** - لفتح على Android (إذا كان متصل)
- **`i`** - لفتح على iOS (إذا كان متصل)

## الطريقة 2: فتح مباشرة في المتصفح

افتح المتصفح واذهب إلى:

```
http://localhost:8081
```

أو

```
http://localhost:19000
```

## الطريقة 3: استخدام Expo Go على الموبايل

1. ثبت تطبيق **Expo Go** من:
   - [App Store](https://apps.apple.com/app/expo-go/id982107779) (iOS)
   - [Play Store](https://play.google.com/store/apps/details?id=host.exp.exponent) (Android)

2. شغل التطبيق من Terminal:
   ```bash
   npm start
   ```

3. امسح QR Code الذي يظهر في Terminal باستخدام:
   - **iOS**: كاميرا الآيفون
   - **Android**: تطبيق Expo Go

## الطريقة 4: تشغيل على محاكي (Android/iOS)

### Android:
```bash
npm run android
```

### iOS (Mac فقط):
```bash
npm run ios
```

### Web:
```bash
npm run web
```

## 🔧 إذا لم يعمل التطبيق

1. تأكد من أن Expo يعمل:
   ```bash
   npm start
   ```

2. إذا ظهرت أخطاء، جرب:
   ```bash
   npm install --legacy-peer-deps
   npm start
   ```

3. امسح الكاش:
   ```bash
   npx expo start --clear
   ```

## 📱 الروابط السريعة

- **Web**: http://localhost:8081
- **Metro Bundler**: http://localhost:19000
- **Expo DevTools**: http://localhost:19002

## ✅ قاعدة البيانات جاهزة!

قاعدة البيانات تم إعدادها بالفعل، يمكنك:
- تسجيل حساب جديد
- تصفح المنتجات (5 منتجات تجريبية موجودة)
- إضافة منتجات للسلة
- إنشاء طلبات

---

**ملاحظة**: تأكد من أن قاعدة البيانات تم إعدادها من Supabase Dashboard قبل الاستخدام.

